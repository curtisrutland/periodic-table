import { styled } from "@material-ui/styles";

const Page = styled("main")({
  height: "100%",
  width: "100%",
  display: "flex",
  flexFlow: "column nowrap",
  alignItems: "strech"
});

export default Page;